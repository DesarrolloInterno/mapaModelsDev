type country {
    id: ID
    name: String
    code: String
}

type Query {
    GET_ALL_COUNTRIES: [country]
    GET_COUNTRY(countryCode: String!): [country]
    GET_ALL_COUNTRIES_INTERNATIONAL_FORM: [country]
}